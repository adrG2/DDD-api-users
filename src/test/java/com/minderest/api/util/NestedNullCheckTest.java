package com.minderest.api.util;

import org.junit.Test;

public class NestedNullCheckTest {

    @Test
    public void testResolve() {

	// NestedNullCheck.resolve(() -> user.getAddress().getAccount());
	// assertEquals(Optional.empty(), resolve);
    }

}
